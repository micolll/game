#ifndef UTILITIES_H    // To make sure you don't declare the function more than once by including the header multiple times.
#define UTILITIES_H

void initTermios();
void resetTermios();
char get_input();

#endif
